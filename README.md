# intent-classification-chatbot
A simple intent-based chatbot built with PyTorch and NLTK using a Bag-of-Words approach and a feed-forward neural network (MLP) for intent classification and rule-based responses.
